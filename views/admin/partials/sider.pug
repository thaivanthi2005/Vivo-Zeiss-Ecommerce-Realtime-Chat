.sider
  .inner-menu
    span.sider-label Menu
    ul
      li
        a(href=`${prefixAdmin}/dashboard`) Dashboard
      li
        if(roles.permissions.includes("products_view"))
          a(href=`${prefixAdmin}/products`) Sản phẩm
      li
        if(roles.permissions.includes("products-category_view"))
          a(href=`${prefixAdmin}/category`) Danh mục
      li
        if(roles.permissions.includes("orders_view"))
          a(href=`${prefixAdmin}/orders`) Đơn hàng
      li
        if(roles.permissions.includes("roles_view"))
          a(href=`${prefixAdmin}/roles`) Nhóm quyền
      li
        if(roles.permissions.includes("roles_permissions"))
          a(href=`${prefixAdmin}/roles/permissions`) Phân quyền
      li
        if(roles.permissions.includes("roles_permissions"))
          a(href=`${prefixAdmin}/accounts`) Tài khoản
      li
        if(roles.permissions.includes("roles_permissions"))
          a(href=`${prefixAdmin}/settings/general`) Cài đặt
